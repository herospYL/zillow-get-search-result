// Generated by CoffeeScript 1.11.1
(function() {
  Polymer({
    is: 'zillow-main-result',
    properties: {
      validSearchResults: {
        type: Array,
        value: function() {
          return [];
        },
        observer: 'addingSearchResultChildren'
      },
      validSearchSelected: {
        type: Number,
        notify: true
      }
    },
    addingSearchResultChildren: function() {
      var domItem, i, item, j, len, len1, old, oldChildren, parent, ref;
      if (this.validSearchResults) {
        parent = this.$.listbox;
        oldChildren = Polymer.dom(parent).childNodes;
        if (oldChildren.length > 0) {
          for (i = 0, len = oldChildren.length; i < len; i++) {
            old = oldChildren[i];
            Polymer.dom(parent).removeChild(old);
          }
        }
        ref = this.validSearchResults;
        for (j = 0, len1 = ref.length; j < len1; j++) {
          item = ref[j];
          domItem = document.createElement('zillow-main-result-item');
          domItem.address = item.address.street;
          domItem.city = item.address.city;
          domItem.state = item.address.state;
          domItem.zip = item.address.zipcode;
          Polymer.dom(parent).appendChild(domItem);
        }
        return Polymer.dom.flush();
      }
    }
  });

}).call(this);

//# sourceMappingURL=zillow-main-result.js.map
