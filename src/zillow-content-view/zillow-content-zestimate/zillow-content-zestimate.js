// Generated by CoffeeScript 1.11.1
(function() {
  Polymer({
    is: 'zillow-content-zestimate',
    properties: {
      zestimateData: Object,
      zestimateKind: String
    },
    observers: ['valueChangeColor(zestimateData.valueChange.__text)'],
    getCurrencySign: function(currency) {
      switch (currency) {
        case "USD":
          return '$';
        default:
          return '';
      }
    },
    valueChangeColor: function(value) {
      var valueDiv, valueDivColor;
      valueDiv = this.$['zestimate-change-value'];
      valueDivColor = "";
      if (this._isPositive(value)) {
        valueDivColor = "green";
      } else {
        valueDivColor = "crimson";
      }
      valueDiv.style.borderColor = valueDivColor;
      valueDiv.style.color = valueDivColor;
    },
    _amountSeparate: function(amount) {
      var count, cursor, len, prefixMark, ret, trimAmount;
      if (typeof amount === 'string' && amount !== null) {
        len = amount.length;
        prefixMark = "";
        trimAmount = amount;
        if (isNaN(parseInt(amount.charAt(0)))) {
          prefixMark = amount.substring(0, 1);
          trimAmount = amount.substring(1);
        }
        if (isNaN(parseInt(trimAmount))) {
          return amount;
        } else {
          ret = "";
          cursor = len - 1;
          count = 0;
          while (cursor >= 0) {
            ret = trimAmount.charAt(cursor) + ret;
            if (count % 3 === 2) {
              ret = ',' + ret;
            }
            cursor--;
            count++;
          }
          if (ret.charAt(0) === ',') {
            ret = ret.substring(1);
          }
          ret = prefixMark + ret;
          return ret;
        }
      } else {
        return amount;
      }
    },
    _isPositive: function(amount) {
      if (typeof amount === 'string' && amount !== null && amount.length > 0 && amount.charAt(0) === '-') {
        return false;
      }
      return true;
    }
  });

}).call(this);

//# sourceMappingURL=zillow-content-zestimate.js.map
